=== Shape A: EXPLAIN (FORMAT JSON) — returns TEXT plan, NOT JSON (CORRUPTION) ===
EXPLAIN (FORMAT JSON) SELECT 1;
QUERY PLAN
Project [1]
  DualScan
=== Shape B: EXPLAIN (FORMAT XML) — returns TEXT plan, NOT XML ===
EXPLAIN (FORMAT XML) SELECT 1;
QUERY PLAN
Project [1]
  DualScan
=== Shape C: EXPLAIN (FORMAT YAML) — returns TEXT plan, NOT YAML ===
EXPLAIN (FORMAT YAML) SELECT 1;
QUERY PLAN
Project [1]
  DualScan
=== Shape D: EXPLAIN (FORMAT TEXT) — control case, matches by accident ===
EXPLAIN (FORMAT TEXT) SELECT 1;
QUERY PLAN
Project [1]
  DualScan
=== Shape E: EXPLAIN (FORMAT JSON, VERBOSE) — multi-option, both dropped ===
EXPLAIN (FORMAT JSON, VERBOSE) SELECT 1;
QUERY PLAN
Project [1]
  DualScan
=== Shape F: EXPLAIN (COSTS off, FORMAT JSON) — boolean-arg + FORMAT both dropped ===
EXPLAIN (COSTS off, FORMAT JSON) SELECT 1;
QUERY PLAN
Project [1]
  DualScan
=== Shape G: EXPLAIN FORMAT=JSON (MySQL top-level form on literal SELECT) — TEXT ===
EXPLAIN FORMAT=JSON SELECT 1;
QUERY PLAN
Project [1]
  DualScan
=== Shape H: EXPLAIN (FORMAT TEXT, ANALYZE TRUE) — ANALYZE dropped, query NOT run ===
EXPLAIN (FORMAT TEXT, ANALYZE TRUE) SELECT 1;
QUERY PLAN
Project [1]
  DualScan
=== Shape I: EXPLAIN (ANALYZE, BUFFERS) — both dropped on DualScan target ===
EXPLAIN (ANALYZE, BUFFERS) SELECT 1;
QUERY PLAN
Project [1]
  DualScan
=== SUPPORT pin: bare EXPLAIN SELECT 1 — works, control case ===
EXPLAIN SELECT 1;
QUERY PLAN
Project [1]
  DualScan
